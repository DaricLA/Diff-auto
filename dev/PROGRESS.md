# PROGRESS 开发进度卡（新对话/新设备先粘贴本文件）

## 项目
Excel 差异审计工具（openpyxl 底层 vs Excel 显示层）+ 自动化开发闭环。
场景：测试机离线/无权限/仅免安装exe；开发在手机或电脑（AI+GitHub）；GitHub Actions 云端编译；U盘传数据。

## 最新状态（2026-09-06 待上传批次：第十四~十五批 + 补1~4，均已本机验证，待实机确认）
- UI：列宽显式管理(_auto_layout_columns hold 锁定/位置类型先压) / 进度条16px+E3中心迸发+统一#18BC9C绿
  / 全局白底 / 日志已豁免改黑粗
- 高级规则：数据趋势=历史矩形(空忽略0计入)+阈值双模式互斥(行列抓取或数值)；文件名一致性=模板纯格式
  检查(空段/*任意+文字段必须相等)+分段start/end可自定义，两开关独立；文件名窗口修复(崩溃/居中/按钮)
- 这些改动**全部在 main.py**（4332行）；cli.py/ai_summary.py/regression.py 未动；
  主引擎判定逻辑零改动（趋势/文件名引擎除外——属新增/重构检查逻辑，已单测）
- 上传后待测：规则配置三个弹窗(文件名一致性/数据趋势)逐项点验；真实 PBO 文件跑一遍完整对比

## 里程碑（2026-09-06 全部达成 ✅）
✅ 金标准回归 14/14
✅ L2 全场景 149/149 全绿（B1 4/4、B2 115/115、B3 10/10、B4 12/12、B5 8/8）
✅ 业务拍板全部落地并验证：行高=默认15/列宽=默认8.43豁免、字体别名归一、主题色按显示色判定
✅ 主要修复：CheckProject参数错位、value开关消费、数组公式门控、B4/B5工作簿混用、
   主题色索引(accent2)、断言语义体系(DIFF=存在/SAME=豁免/NODIFF=无差)、图片tempfile、诊断已删

## 仓库与文件（GitHub: Diff-auto, Public）
- main.py        v4.0（4332行；2026-09-06 下半场大版本：列宽/进度条/趋势/文件名/白底）
- gui_main.py / gui_patch.py   测试按钮 v2.2 已验证（未动）
- cli.py / ai_summary.py       auto_run.exe（FEEDBACK 阶段再动）
- regression.py  单文件（约1157行）：金标准14 + L2 149用例，输出 RULES_VERDICT.txt + L2_REPORT.txt
- build.yml      已清 l2_scenarios；main.py 改动需全量重建3个exe
- dev/PROGRESS.md / dev/STRUCTURE.md

## 下一步（全部完成，进入日常使用）
✅ VERSION 升 v3.99（已验证，报告显示版本 v3.99）
✅ 2026-09-06 下半场大版本升级 → **v4.0**（用户拍板：改动量大必须升版本；main.py L14，报告头部/标题栏显示 v4.0）
✅ COM 观察标签改为引擎实际结果（已验证：行高15/列宽8.43 显示"程序: 无差"）
✅ 开发闭环完成：引擎+测试+规则+豁免+GOLD/FEEDBACK 流程全部就绪
- 日常使用：业务文件跑 auto_run/GUI；发现问题在 FEEDBACK.txt 填一行 → AI 自动分析

## 2026-09-06 第二批：UI/进度/日志优化（已改 main.py，待回归验证）
- 差异详情着色：规则命中时 描述=浅灰、规则名=加粗、结果行 → 红粗（on_tree_select L3910）
- 进度条：①全局单调防回退（update_progress _prog_last）；②跨 sheet 连续进度（_proc_pct）；
  ③indeterminate 改伪动画大块 15%~70% 循环（原生块宽无法配置，块宽>150px）；
  ④流程全覆盖：加载5-20 / 对比25-80 / 插件85 / 高级检查87（新增）/ 规则过滤92 / 报告95 / 100
- ⏳ 心跳补齐：逐sheet对比、插件、高级检查、COM采集、规则过滤（引擎 _with_heartbeat pulse 参数；
  GUI _gui_heartbeat）；心跳行原地刷新并被完成行顶替（log 残影 bug 已修）
- COM 阶段改动画+心跳，不再把进度从100拉回0；verifier 不传 progress 回调
- 判定逻辑零改动（纯 UI/进度/日志层）；STRUCTURE 行号已刷新（main.py 4009行）
- 本机验证（Python 3.13.15）：py_compile ✓ / import main ✓ / 引擎全链路冒烟（进度单调 5→100）✓
  / _with_heartbeat pulse 分支 ✓ / _proc_pct 全局进度 ✓ / update_progress 单调 ✓ / 伪动画循环与恢复 ✓ / log 心跳顶替无残留 ✓
- 修复2个bug：① _prog_last 跨次检查不重置导致第二次进度卡死 → start_compare 重置归零；
  ② 伪动画起始值 clamp（避免从满格直接跳回15）→ 按当前进度值起步（15~70范围）
- ✅ 回归验证通过（2026-09-06 测试机）：金标准 14/14 + L2 149/149 全绿（B1 4/4、B2 115/115、
  B3 10/10、B4 12/12、B5 8/8），无硬断言失败；COM 观察点均为已知观察项
  （tint/主题色内部色号差异等，判定不受影响）；行高15/列宽8.43 显示"程序: 无差"（豁免生效）
- 状态：UI/进度/日志优化已完成并入库，进入日常使用

## 2026-09-06 第三批：进度条 Canvas 自绘重做（已改 main.py，待回归+GUI实机看效果）
- 用户拍板：方案D 整条同色 橙#FF9800→绿#198754 随进度渐变；呼吸=亮度±14%(amp0.28,周期1.58s)；
  高度33px(与原ttkbootstrap一致)；纯色无条纹；完成时呼吸停止固化纯绿
- 技术验证：ttkbootstrap flatly 主题下 style.configure(background) 不生效（实验证明）→
  改为 Canvas 自绘（_prog_color L792 / _rr L799 / _prog_cv L3338 / _prog_tick L3578）
- 0~100单调平滑：update_progress 仅单调 target；30ms插值动画消除阶梯；删伪动画/_pulse_*
- 全流程进度映射（不回退）：加载5→20/对比25→80/插件85/高级87/报告95/对比完成96/
  COM采集97→99(verifier进度经_com_map映射)/规则过滤99/完成100（GUI收尾发）
- 本机验证：py_compile ✓ / _prog_color 色值断言 ✓ / Tk真实窗口动画测试（单调、平滑、呼吸
  启停、_draw_progress）✓ / 引擎冒烟：进度 5→15→20→52→80→95→96 单调 ✓
- 判定逻辑零改动；regression.py 内嵌副本不受影响；STRUCTURE 行号已刷新（4035行）

## 2026-09-06 第四批：进度分配重排 + 呼吸全程不断（已改 main.py，待回归+实机确认）
- 用户反馈：①慢阶段%占比太少（COM 只2%、过滤1%）→ 按耗时权重重排映射：
  加载5→22 / 对比25→70(45%) / 插件72 / 高级74 / 报告76 / 对比完成78 /
  COM采集80→93(13%，每sheet更新) / 规则过滤93→98(蠕动0.15%/s) / 完成100
- 用户反馈：②进度卡住时呼吸不能停 → _prog_tick 循环条件改为 breathe 常真（breathe=True 恒循环），
  仅 on_comparison_finished（最终完成）才停；新增 _prog_creep 慢阶段时间锚定蠕动
- 本机验证：py_compile ✓ / 引擎冒烟 5→15→22→47→70→76→78 单调 ✓ /
  Tk 实测：进度停住 anim 仍运行 ✓ creep 推进不超上限 ✓ 完成才停 ✓ 卡住时呼吸颜色持续变化 ✓
- 判定逻辑零改动；STRUCTURE 行号已刷新（4047行）

## 2026-09-06 第五批：呼吸改版（橙↔绿循环）+ 完成闪烁提醒（已实现，待回归+实机确认）
- 用户拍板：①未满进度时颜色改为 橙#FF9800↔绿#198754 全跨度循环呼吸（相位0→1→0往返约1.6s，
  明显可见；进度语义由条长度表达）；②到达终点高亮闪烁提醒=方案1脉冲闪烁（亮#7cffc9↔暗#0d5a3c
  0.3s/拍×4次共2.4s，之后定格#198754）——原闪烁色与基绿对比太小不可见，已优化参数
- 实现：_prog_color 改为按 breath 插值（frac 不再影响颜色）；_prog_tick 加入闪烁序列
  （_prog_flash 8拍 + _prog_flash_wait 节拍器）；_draw_progress 三态着色
- 本机验证：py_compile ✓ / 呼吸端点断言（0=#ff9800、1=#198754）✓ / 相位往返循环 ✓ /
  闪烁8拍后动画停止 ✓ / 定格色 #198754（canvas fills 实测）✓
- 判定逻辑零改动；STRUCTURE 已同步

## 2026-09-06 第六批：完成提醒改为方案B 光晕脉冲（已实现，待回归+实机确认）
- 用户拍板：完成提醒=方案B（macOS 风光晕脉冲）：内部稳定绿，外围三色发光圈
  （#d6fbe9→#a8fbdc→#7cffc9）扩散 2 次（0.6s/脉冲，30ms/帧共40帧=1.2s）后定格 #198754
- 实现要点：canvas polygon 不支持 width 描边（TclError 被吞导致早期不可见）→ 改用 3 层
  填充色内缩模拟渐隐扩散；_prog_tick 光晕计数 _prog_flash 0..40
- 本机验证：py_compile ✓ / 中间帧 fills=[浅3层+槽+绿] ✓ / 40帧后动画停 ✓ / 定格无光晕 ✓
- 判定逻辑零改动；STRUCTURE 已同步

## 2026-09-06 第七批：进度停滞时颜色循环持续（断链修复，已实现，待回归+实机确认）
- 用户反馈：进度停滞/不动时颜色循环停止。根因：_prog_tick 续期逻辑在 try 内，except 吞异常
  后动画链永久中断（停滞阶段无新 progress 回调 → 无法复活）
- 修复：续期重构为 keep 标记——推进中/呼吸中/光晕未完 或 任何异常 都 keep=True 续期；
  （progress 停滞时 breathe=True 恒循环，颜色持续橙↔绿呼吸）
- 本机验证：停滞4.8s无回调 相位120样本持续变化 ✓ / 模拟绘制异常5次链不断 ✓ /
  完成光晕40帧后停止 ✓ / 引擎冒烟 5→15→22→47→70→76→78 单调 ✓
- 判定逻辑零改动；STRUCTURE 已同步

## 2026-09-06 第八批：颜色恒定循环 + 末段过渡绿（已实现，待回归+实机确认）
- 用户要求：①颜色循环不受进度影响（恒定循环）——呼吸相位本就独立，已确认无跳变；
  ②仅最后一小段（96→100）从呼吸色过渡到绿（新 _mix_hex 混合）
- 关于"规则过滤停滞时颜色没播放"：本机模拟18s停滞 颜色92样本持续呼吸 ✓ 未复现——
  用户实机看到的应是旧版本（此前修复尚未上传构建），请上传后实测
- 本机验证：py_compile ✓ / _mix_hex 端点 ✓ / 30%→80% 相位连续无跳变 ✓ /
  96%#448a44→98%#2e884c→100%#198754 过渡 ✓ / 停滞6s呼吸92样本 ✓
- 判定逻辑零改动；STRUCTURE 已同步

## 2026-09-06 第九批：双循环架构（独立呼吸计时链，已实现，待回归+实机确认）
- 用户拍板方案：呼吸与进度彻底解耦——
  ①呼吸循环（独立 after 链，60ms 帧）：相位 = sin(真实时间×2π/1.6s)，time.time() 驱动不数帧，
    与进度回调/事件延迟完全无关，进度停滞多久都恒定播放；仅最终完成（on_comparison_finished）停止
  ②进度循环（_prog_tick 30ms）：宽度插值/蠕动/光晕计数/96→100 过渡系数，不再碰呼吸相位
  ③绘制合并：颜色优先级=光晕>纯绿>呼吸色(96-100 mix 绿)>纯呼吸
- 呼吸周期定版 1.6s（原 4.8s 太慢——帧计数步进 0.0125/帧 实为 4.8s，已修正）
- 本机验证：进度停10s 呼吸60色持续循环 ✓ / 进度链断呼吸链独立循环 ✓ /
  完成双链停止+flash40 ✓ / 96%#8b8f2a→100%#198754 ✓ / 引擎冒烟无回归 ✓
- 判定逻辑零改动；STRUCTURE 已同步；新增 import math；_start_breath_loop/_breath_tick

## 2026-09-06 第十批：过滤性能修复（分钟级→5.9秒）+ 慢阶段进度可见（已实现，待回归+实机确认）
- 用户实测：卡在"懒加载缓存"——真相：懒加载仅0.6s；真正杀手是 _get_cfs_for_cell 对 4377 diff
  × CF 规则反复构造 MultiCellRange（openpyxl 昂贵），worker CPU 满载分钟级；主线程其实正常
- 修复A：_get_cfs_for_cell sqref→MultiCellRange 缓存（按 ws 存 dict）→ 过滤 5.9s（真实验证）
- 修复B：_apply_rule_filter 每 200 条 diff 报进度（90→98 渐进）
- 修复C：_find_anchor max_row/max_column 提出循环（openpyxl 全表计算属性，每行重复扫描是热点）
- 慢阶段进度可见：蠕动 0.15%/s → 1.0%/s；COM 80→90；过滤 90→98（8% 区间）
- 本机验证：全流程 8.7s（引擎2.2+COM0.6+过滤5.9）探针零卡顿 ✓ / 蠕动≈1%/s ✓ /
  过滤进度回调峰值98 ✓ / 停滞呼吸持续 ✓ / py_compile ✓
- 判定逻辑零改动；STRUCTURE 已同步

## 2026-09-06 第十一批：进度映射按实测耗时比例重排（接近匀速，已实现，待回归+实机确认）
- 用户要求：慢的地方多分、整体接近匀速——按真实复现耗时比例（总8.7s：加载1.3/对比0.9/COM0.6/过滤5.9/收尾）
  映射：加载4→15(13%) / 对比15→25(10%) / 插件25 高级26 报告27 对比完成28 /
  COM 28→35(7%) / 规则过滤35→98(63%，每200条diff回报) / 完成100
- 真实曲线验证（152MB+32规则）：0~2.2s 走4→28（13%/s），2.2~8.7s 走28→95（11%/s）——接近匀速 ✓
- 过滤段 GUI 侧 1%/s 蠕动 + 30ms 插值兜底平滑；总耗时 8.7s 不变
- 判定逻辑零改动；STRUCTURE 已同步

## 2026-09-06 第十二批：公式缓存值快读（替代二次加载152MB）+ COM 回滚（已实现，待回归+实机确认）
- 用户纠正：懒加载公式缓存值只是 shift 比对取数，与 COM/检查无关——回滚全部 COM 线程化改动
  （jump_to_excel / ComCheckDialog 恢复原样，COM 显示层调取逻辑零改动）
- 新方案 _read_formula_cache：流式解析 sheet XML 只取公式<f>的<v>缓存值（zipfile+lxml.iterparse），
  不二次加载整个工作簿——152MB 文件 0.07s（原 data_only 全量 0.58s，用户机器更慢更明显）
- 一致性验证：2228 公式格归一比较（日期序列 vs datetime、数值、字符串、共享字符串索引）0 真实不一致；
  失败时回退 load_workbook(data_only=True) 双保险；_formula_cache_lookup 兼容双形态
- 引擎懒加载点改 4 行（_load_old_vals/_load_new_vals + 2 处取值）；判定逻辑零改动
- 分阶段耗时日志保留（comfix 批次中与 COM 无关的部分）：检查完成后输出
  "分阶段耗时：加载+对比 / COM采集 / 规则过滤 / 收尾"（下次实测可量化定位任何卡点）

## 2026-09-06 第十三批：诊断导出清理（已实现，待回归+实机确认）
- 用户拍板：导出诊断早已无用 → 删除 _finalize_diag（自动落盘 check_diag_*.json）/
  export_diag（手动导出）/ 工具栏"导出诊断"按钮 / worker 3 处调用 / _diag 属性；
  异常路径保留报错弹窗；预处理失败改普通 log
- 用户实测确认（上一版本成功日志）：懒加载快读后 "✓ 旧版缓存加载完成" 秒级；
  全流程 9s（加载+对比2 / COM 1 / 过滤4 / 收尾0）；103 项人工复核 + 3878 豁免（规则3876+样式2）
- 过滤 4 秒=真实耗时（用户暂搁置进度节奏问题，映射保持：过滤 35→98 63% 区间）
- 行号已刷新（STRUCTURE 更新为 4187 行版本）；判定逻辑零改动
- 用户要求：慢的地方多分、整体接近匀速——按真实复现耗时比例（总8.7s：加载1.3/对比0.9/COM0.6/过滤5.9/收尾）
  映射：加载4→15(13%) / 对比15→25(10%) / 插件25 高级26 报告27 对比完成28 /
  COM 28→35(7%) / 规则过滤35→98(63%，每200条diff回报) / 完成100
- 真实曲线验证（152MB+32规则）：0~2.2s 走4→28（13%/s），2.2~8.7s 走28→95（11%/s）——接近匀速 ✓
- 过滤段 GUI 侧 1%/s 蠕动 + 30ms 插值兜底平滑；总耗时 8.7s 不变
- 判定逻辑零改动；STRUCTURE 已同步

## 2026-09-06 第十四批：列收缩修复 + 进度条 16px + 完成动画改为 E3 中心迸发（已实现，待实机确认）
- 用户反馈①：左右缩小主界面时先压缩 Sheet，位置/类型列不动 → 根因：Tk Treeview 自动布局只压缩
  stretch 列；position/type 是 stretch=False，Tk 永远不会压缩它们，旧 _on_root_resize（minwidth 80↔200
  切换）是死代码（条件永不触发）
- 修复：显式列宽管理 _auto_layout_columns（L3529，绑定 tree <Configure>）——位置/类型按宽度比例
  并行压缩 → 压没后 Sheet 才压缩（min 80）→ 收起列恒定 60 → 窗口放大自动恢复默认(340/130/130)
- 用户反馈②：进度条变细一半 → _prog_cv height 33→16（m=2, r=min(8,...)）
- 用户反馈③（实机）：Sheet 初始列宽 340 太小、信息挤左侧 → 满宽态改为 Sheet 动态吃满剩余空间
  （1100px 宽时 ≈758px）
- 用户反馈④（实机）：缩小窗口时 Sheet 跟随缩（先等比到 340 才压位置/类型）→ 压缩阈值改用 hold 锁定：
  窗口一小于 hold+320 立即压缩位置/类型（Sheet 冻结不动），位置/类型压没后 Sheet 才压（hold→80）
- 本机验证：1100→1000/900：Sheet 758 冻结、位置/类型 130→80→30 ✓；位置/类型压没后 Sheet 758→138 ✓；
  恢复满宽还原 ✓（判定逻辑零改动）
- 用户反馈③：完成提醒改为「长条曝光」方案——拍板 E3 中心迸发：白芯从中心向两端铺满（15帧0.45s）
  → 整条泛白回落（18帧0.54s，ease-in-out cos）→ 定格 #198754；flash 计数 40→33；替换原光晕脉冲
- 色号核对：GIF 演示与 main.py 一致（橙#FF9800/绿#198754/槽#e9ecef），实现直接用 _mix_hex('#198754','#ffffff',k)
- 本机验证：py_compile ✓ / Tk 实测 canvas height=16 ✓ / flash 0→33 停止 ✓ / 迸发期白绿混合帧出现 ✓ /
  结束帧 slot+绿 ✓ / 引擎冒烟（152MB 真实文件）run()=True 2.3s，差异分布与基线一致（4277内容/48条件格式/34图片…）
- 判定逻辑零改动；STRUCTURE 已同步（4208行）

## 2026-09-06 第十五批：趋势配置改造 + 日志着色 + 白底（已实现，待实机确认）
- 用户反馈①：日志“已豁免 3878 项”蓝色加粗 → 黑色加粗（log_blue_bold→log_bold，L3772 段）
- 用户反馈②：数据趋势配置——管制阈值/USL/LSL 不再人工输入，改为「基于锚点的行/列偏移」从表格
  抓取单元格值；历史数据范围去掉行数/列数（锚点+行偏移+列偏移定起点，范围自动扩展：起点列向下
  连续数值，为空则起点行向右）；UCL/LCL 特殊模式值同样抓取；α 仍手输；旧配置（数字/mean_range
  列表）兼容
- 用户反馈③：对话框加大 520x680→680x900、白底；修复按钮区被 expand 内容区挤掉（btn 先 pack
  side=bottom）、内部帧宽度硬编码 480→自适应、canvas 白底
- 全局白底：style TFrame/TLabelframe/TLabel background='#ffffff' + root 白（主界面+对话框）
- 本机验证：py_compile ✓ / 引擎单测（构造表格：均值/标准差/最大值/CPK 从 B12/B13/E12…抓取触发、
  最小值不触发、旧格式兼容、UCL特殊抓取触发）✓ / 对话框截图验证（按钮可见、内容撑满、白底）✓ /
  152MB 冒烟 PASS（判定零改动）
- STRUCTURE 已同步（4278行）；构建需全量重建 3 exe

## 2026-09-06 第十五批补：文件名一致性对话框修复（已实现，待实机确认）
- 用户反馈：点"配置"后界面无控件、卡住关不掉、抓不到文件名、不居中 → 根因：
  ①seg_tv 列宽循环 `for c,w,h in [('idx',40)...]` 二元组解包 3 变量 → 构造中途 ValueError，
  窗口建一半崩溃（单选可见、后面控件/按钮/center_window/wait_window 全部未执行）
  ②"读取当前文件名/解析"按钮被 expand Entry 挤掉（同趋势对话框按钮坑）
  ③窗口 600x550 高不够，底部映射行被截断
- 修复：解包 (c,w)；按钮先 pack(side=right)再 Entry；窗口 620x620、分段树 height 8；
  center_window 增加父窗口几何不可用时回退屏幕居中保护
- 本机验证：构造无异常 ✓ / 位置居中（fdlg 中心=父窗口中心）✓ / _current_filename 抓取
  新文件名（去扩展名）✓ / 解析 8 段 ✓ / 设置映射+确定→segments 结构正确 ✓ / 截图确认
  按钮/映射行/取消确定完整可见 ✓

## 2026-09-06 第十五批补2：趋势阈值双模式互斥 + 历史矩形恢复（已实现，待实机确认）
- 用户反馈①：历史数据恢复 行偏移/列偏移+行数/列数 矩形；矩形内空单元格忽略不计、数值 0 正常统计
  （bool 排除）→ _collect_hist 矩形取值恢复，说明文字更新
- 用户反馈②：阈值输入支持「行/列偏移(从表格抓取)」或「直接数值」双模式互斥：填行列→数值框禁用，
  填数值→行列禁用，全空=不检查该项；UCL/LCL 特殊模式同样支持；_thr 支持 dict{'value':x}
- 引擎实测：矩形含[0,2,None,4,'',0]→有效[0,2,4,0]✓；value 模式阈值生效✓；cell 模式✓；
  旧格式兼容✓；UI 互斥禁用实测✓（填行列→值disabled、填数值→行列disabled）；回填✓
- 截图确认界面完整；py_compile ✓

## 2026-09-06 第十五批补3：文件名一致性 v3（已实现，待实机确认）
- 用户反馈①：分段支持按「起始~结束字符位」截取（1=第1字符，含端点）；解析按钮自动按 _ 拆分并
  计算每段 start/end（实测 8 段位置全对）
- 用户反馈②：去掉"模板/分段二选一"模式单选 → 两个独立开关（模板检查+分段检查同时生效，
  均可开关）；模板=检查文件名是否符合预设模板（{字段}占位+字段映射），分段=按字符位截取比对
- 引擎：template_enabled/segment_enabled 门控（旧配置缺省=True 兼容）；start/end 优先取数，
  无则按 index 拆分（旧格式兼容）；实测用例①start/end 截取匹配/报警正确 ②index 旧格式 0 条 ③模板
  填充触发
- UI 细节修复：srow 按钮先 pack（读取当前文件名/解析 可见）；680x780 滚动白底；确定/取消固定底部
- py_compile ✓ / 截图验证 ✓

## 2026-09-06 第十五批补4：文件名一致性 v4（已实现，待实机确认）
- 用户反馈①：模板检查简化——纯格式检查，不读表格：模板按 _ 分段，空段/*=该段任意，
  文字段=实际文件名该段必须相等，段数须一致；示例说明文字显示在输入框下方
  （如 "7.Krios2__Machine#3301_Rev5.0_*"）；field_mappings 废弃（_ok 保留空列表兼容）
- 用户反馈②：分段 start/end 可自定义——编辑行增加「起始/结束」输入框；选中分段自动回填
  start/end/sheet/cell；点设置映射按自定义位置截取比对
- 引擎实测：空段任意✓/星号任意✓/段数不符报警✓/固定段不符(描述含第N段)✓/开关关闭不检✓
- UI 实测：选中回填✓、自定义 25~30 映射✓、_ok 结构✓；py_compile ✓

## 关键约定（易踩坑）
- 非程序员：代码修改必须给【行号+原文+替换文本】，代码块内无行号（手机无法搜索）
- main.py 改动需用户拍板且零行为变化+回归验证
- 测试机：离线/无权限/无Python；跑前先手动打开 Excel 关激活弹窗；
         跑 regression 前须关闭 Excel 中打开的 l2_* 文件（否则 Permission denied）
- 回传小文件：AI_PACKAGE.txt / RULES_VERDICT.txt / L2_REPORT.txt；DIAG_FULL.json 留测试机
- 差异类型名：值变化=「内容变化」；Sheet 名可能含尾部空格
- shift：垂直范围必须合法非空否则跳过；old列+shift_offset=新列
- 业务结论：Oven profile 整批+6天 = 预期差异（已拍板）

## 换对话交接（下一步就做）
1. 用户确认开始 FEEDBACK.txt / GOLD.md 阶段 → AI 起草模板 → 用户填写/确认 → 入库
2. 贴回 PROGRESS + STRUCTURE（均为最新）
