# main.py 2026-09-09 改动说明（v4.2）

> 用途：与不同版本 main.py（v4.0 / v4.1）做代码比对时的差异说明。
> 版本：main.py v4.2，总行数 4595（VERSION = "v4.2"，L14）。
> 今天（2026-09-09）改动 = 文件名一致性 v5 全面重做 + 编辑规则界面滚轮修复 + `_src_path` 标记。
> 文中行号以当前 v4.2 为准；另附 2026-09-08（v4.1 数据限界豁免）改动摘要便于与 v4.0 完整比对。

---

## 一、变更总览（v4.0 → v4.2 全部差异）

| # | 功能 | 类型 | 涉及代码位置（v4.2 行号） |
|---|------|------|---------------------------|
| 1 | 版本号 | 升级 | L14：VERSION = "v4.2" |
| 2 | 统一滚轮机制 `_chat_wheel` | 新增函数 | L187 起（模块级函数，返回 `_chat_wheel(dialog, event)`） |
| 3 | 规则编辑窗滚轮修复 | 修改 UI | RuleEditorDialog：L3671（bind_all）、L3696（数据源区标记）、L3712（检查项区标记）；**删除**了旧的 `_ds_wheel` Enter/Leave 绑定段 |
| 4 | 数据趋势/文件名配置窗滚轮统一 | 修改 UI | DataTrendConfigDialog：L3426（标记）、L3547（bind_all）；FileNameCheckConfigDialog：L3239（标记）、L3295（bind_all） |
| 5 | 文件名一致性引擎 v5 | **全面重写** | FileNameCheckEngine L1230；新增 `_parse_date_seg` L1302、`_cell_to_date` L1312 |
| 6 | 文件名一致性配置窗 v5 | **全面重写** | FileNameCheckConfigDialog L3222（原 L3161 旧版整窗替换） |
| 7 | `_src_path` 源路径标记 | 新增（修复空转） | `_load_workbooks` L1677 内，L1685：`old_wb._src_path=...; new_wb._src_path=...`；引擎读取 L1239 |
| 8 | （v4.1 批次）数据限界豁免 | 上批新增 | `_build_waiver_map` L1784、`_waiver_lim` L1805、`_waiver_judge` L1827、`_apply_rule_filter` L1843 豁免分支、LimitWaiverConfigDialog L3589 |

**未改动（比对时可确认与 v4.0 一致）**：列宽/行高检测（`_compare_row_col_dimensions` L2309）、Excel COM 采集（ExcelCOMVerifier L2777 / collect_style_data L2803）、单元格对比（`_compare_worksheet` L2127 / `_get_cell_diff` L2220）、样式对照、进度条/日志等全部主引擎及显示层逻辑。

---

## 二、功能 1：编辑规则界面（含检查项界面）滚轮修复

### 需求
编辑规则界面与检查项界面的滚动区，鼠标滚轮无法翻动，只能点击滚动条拖动。

### 根因（旧实现的缺陷）
- **检查项区**（右侧）的 Canvas 从未绑定过滚轮事件；
- **数据源区**（左侧）虽绑定了，但用的是「鼠标进入/离开时 `bind_all`/`unbind_all`」的方式——光标一旦移到按钮、输入框等**子控件**上方，父 Canvas 收到 Leave 事件把绑定解除，导致滚轮实际几乎失效。

### 实现方案
统一滚轮机制，所有带滚动区的对话框共用：

1. **标记**：每个可滚动 Canvas 创建后打标记 `canvas._chat_scroll_cv = canvas`（自身引用）；
2. **常驻监听**：对话框构造时 `self.bind_all('<MouseWheel>', lambda e: _chat_wheel(self, e))`，不再用 Enter/Leave 解绑；
3. **定位滚动**：`_chat_wheel(dialog, event)` 用 `dialog.winfo_containing(x_root, y_root)` 找光标下控件，沿 master 链向上找第一个带 `_chat_scroll_cv` 标记的 Canvas，**且要求该 Canvas 的顶层窗口就是本对话框**（避免与其他窗口的滚轮监听互相干扰），然后 `yview_scroll`；
4. 拖动滚动条功能不变。

### 代码位置
- `_chat_wheel`：L187（约 17 行）
- RuleEditorDialog：L3671（绑定）、L3696（数据源区标记）、L3712（检查项区标记）
- DataTrendConfigDialog：L3426、L3547
- FileNameCheckConfigDialog：L3239、L3295
- 被替换的旧代码（v4.0 存在，v4.2 已删除）：`ds_canvas` 后的 `def _ds_wheel(event)` + `ds_canvas.bind("<Enter>"...)` + `ds_canvas.bind("<Leave>"...)` 三行

---

## 三、功能 2：文件名一致性检查 v5（全面重做）

### 需求（用户拍板 4 点）
1. **废弃旧版**（模板纯格式检查 `format_template` + 分段 `segments` + `template_enabled/segment_enabled` 双开关 + `field_mappings`），不做兼容；
2. **严格相等**：文本比较不允许去首尾空格；
3. **固定 20xx** 补全两位年份；比较符**只做「等于」**；
4. 模板文件名支持「从旧版读取」按钮。

### 新数据结构（config 存续于规则 advanced_engines[filename_check].config）
```json
{
  "current_filename": "NEW_260818_Rev5.0",   // 界面显示用；引擎实际以源路径为准
  "template_filename": "OLD_7.Krios2_Rev2.0", // 模板文件名（手输或从旧版读取）
  "rules": [
    { "start": 1, "end": 5, "op": "eq",
      "target": "template",        // template=模版 | file=待测文件
      "mode": "text",              // text=文本 | date=日期
      "sheet": "Sheet1", "cell": "B2" }      // target=template 时 sheet/cell 固定为空
  ]
}
```

### 界面（FileNameCheckConfigDialog 重写）
- **文件名区**：当前文件名（只读 Entry +「读取」按钮，自动取 parent.new_path/old_path 文件名去扩展名）；模板文件名（可编辑 Entry +「从旧版读取」按钮=old_path 文件名）；
- **规则列表**：Treeview（序号/起始/结束/比较符/目标/模式/Sheet/单元格）+「添加到列表」「更新选中」「删除」；
- **规则编辑行**：起始/结束（几位取几位，含端点，1=第1字符）＋比较符固定显示「等于」＋目标下拉（模版/待测文件）＋模式下拉（文本/日期）＋Sheet 下拉＋单元格输入；
- **联动冻结**：目标=模版 → Sheet/单元格置灰禁用；模式=日期 → 目标自动强制为「待测文件」并禁用目标下拉；
- **选中回填**：点击列表行自动回填编辑行，便于修改。

### 引擎判定逻辑（FileNameCheckEngine.run，L1235）
前置：`rules` 为空 → 不检查返回空；文件名取自 `new_wb._src_path`（见功能 3）。

每条规则独立执行（全部通过=一致，任一失败=产生一条「文件名一致性」告警，advanced_check=True）：

| 目标 | 模式 | 判定 | 报警描述示例 |
|---|---|---|---|
| 模版 | 文本 | 文件名截取段 vs **模板文件名字符串同位置段**，字符级严格相等（不去空格；一边空一边非空=不一致；两边空=通过） | `文件名 第[1~5]位 "26081" ≠ 模板同名位 "26091"` |
| 待测文件 | 文本 | 文件名截取段 vs Sheet/单元格内容 `str(value)`（None→空串；不做任何 strip） | `文件名 第[1~5]位 "26081" ≠ 报告(Sheet1!B2) "26082"（严格相等）` |
| 待测文件 | 日期 | 拆解对比（见下） | `日期不一致: 文件名 "260819"=2026-08-19 ≠ 报告(Sheet1!B3) 2026-08-18` / `无法识别为日期(yyMMdd/yyyyMMdd)` |

### 日期拆解对比算法（功能 3 核心）
- **文件端** `_parse_date_seg`（L1302）：只取数字字符剥离分隔符后按长度识别：
  - 6 位 → `yyMMdd`，年份 = `2000 + yy`（固定 20xx），如 `260818` → 2026-08-18、`991231` → 2099-12-31；
  - 8 位 → `yyyyMMdd`；
  - 其他长度/月日非法 → 返回 None（报警「无法识别为日期」）。
- **单元格端** `_cell_to_date`（L1312）：
  - `datetime`/`date` 直接取日期；日期序列数（int/float）用 `openpyxl.utils.datetime.from_excel` 转换；
  - 文本：剥离时间部分后按多格式尝试解析（yyyy/mm/dd、yyyy-mm-dd、yyyy.mm.dd、yyyyMMdd、yyMMdd、mm/dd/yyyy、yyyy年mm月dd日）；
  - 解析失败/布尔/None → None（报警）。
- 双方都成功 → 比较**年/月/日三元组**（忽略时分秒与显示格式）。

### 代码位置
- 引擎：FileNameCheckEngine L1230（run L1235，`_parse_date_seg` L1302，`_cell_to_date` L1312）
- 配置窗：FileNameCheckConfigDialog L3222（`_current_filename` L3297、`_old_filename` L3305、`_fill_current_filename` L3313、`_fill_old_filename` L3317、`_sync_edit` L3321、`_read_edit` L3341、`_add_row` L3357、`_update_row` L3362、`_del_row` L3371、`_renumber` L3374、`_ok` L3377）
- 废弃（v4.0 存在，v4.2 已删）：旧引擎模板/分段双开关逻辑、旧窗 `tpl_lf`/`seg_lf`/`seg_tv`/`_parse_filename`/`_set_seg_mapping` 等

---

## 四、功能 3：`_src_path` 标记（修复旧文件名检查空转）

### 背景
openpyxl 的 `load_workbook()` 加载后，workbook 对象**没有 filename 属性**（只有 `save()` 会设置）。旧版（含 v4.0）文件名检查用 `new_wb.filename` 取文件名 → 实际一直取到空 → **旧版文件名一致性检查从未真正生效**（这是本次发现的既有缺陷，不是回归）。

### 方案
- `_load_workbooks`（L1677）在 `new_wb` 加载完成后打标记（L1685）：
  `old_wb._src_path = self.old_path; new_wb._src_path = self.new_path`
- 引擎取文件名（L1239）：
  `_src = getattr(new_wb, '_src_path', None) or new_wb.filename 有值时的取值，否则 ''`
  即优先 `_src_path`，回退 `filename` 兼容其他途径构造的 wb。

### 影响
仅文件名一致性检查感知道路修复；不影响任何对比/COM/样式逻辑（多一个对象属性）。

---

## 五、附：v4.1 批次改动摘要（2026-09-08，与 v4.0 比对时也要算入）

- **数据限界豁免**（高级检查区第 4 项，engine_type=`limit_waiver`）：
  UI：RuleEditorDialog 高级检查列表新增「数据限界豁免」项（L3720 列表），配置窗 LimitWaiverConfigDialog L3589；
  引擎：不进 ADVANCED_ENGINE_REGISTRY（`_run_advanced_engines` 自动跳过）；判定在过滤期：`_build_waiver_map` L1784（每规则预处理 UCL/LCL 阈值，支持相对锚点抓取或数值）、`_waiver_lim` L1805、`_waiver_judge` L1827、`_apply_rule_filter` L1843 两个命中分支（shift/非 shift，L1912/L1950 附近）加豁免判断；
  语义：无条件豁免（冻结 UCL/LCL 输入，任意值含文本/删除直接豁免）或范围模式（LCL≤值≤UCL 含边界；只填一侧只查一侧；两侧全空=不豁免防漏报；非数值不豁免；锚点缺失/抓取失败不豁免）；shift 模式 UI 拦截。

---

## 六、验证记录（本机）

| 项目 | 结果 |
|---|---|
| py_compile | ✓ |
| 文件名引擎单测（模板同/异、待测文本一致/不一致、严格空格、日期 datetime/文本/序列数、日期不一致、两位年 2099、文件端/单元格端解析失败、空段两边空、多规则 AND、无规则） | 17/17 |
| 配置窗 GUI 测试（自动读文件名、从旧版读取、模版冻结、日期强制待测、添加/更新/删除、保存结构、回填、滚轮标记+滚动生效） | 17/17 |
| 上批限界豁免回归 | 16/16 |
| 小全链路冒烟（run + 文件名日期规则一致零告警 + 豁免命中） | PASS |

---

## 七、版本间比对速查（v4.0 → v4.2 共 36 处差异块）

所有差异块仅分布在：VERSION 行 / `_chat_wheel` 及滚轮标记绑定 / FileNameCheckEngine 与两个日期方法 / `_src_path` 两行 / 豁免相关函数与分支 / FileNameCheckConfigDialog 整窗 / LimitWaiverConfigDialog 类 / 高级检查列表加项。
**列宽/行高检测、COM 采集、主引擎判定、样式、进度条、日志 代码零改动。**
