# GOLD 预期差异登记表

> 用途：记录"引擎应如何判定"的正式预期（拍板/验证结果）。后续任何报告与这个表对照，
> 不一致即为 bug 或新需求。维护：AI 更新；用户确认拍板项。

## 1. 基础判定（金标准 14/14 已验证）
| # | 场景 | 预期 | 来源 |
|---|---|---|---|
| G01 | 值变化（10→20） | 报「内容变化」 | v01 |
| G02 | 两文件完全相同 | 零差异 | v02 |
| G03 | 数字格式 '0' vs '0.00' | 报「数字格式变化」（值相同也报） | v03 |
| G04 | 数字格式检测关闭（number_format=False） | 不报 | v04 |
| G05 | 字体名/字号变化 | 报「字体变化」 | v05/v06 |
| G06 | 填充色变化 | 报「填充变化」 | v07 |
| G07 | 边框变化 | 报「边框变化」 | v08 |
| G08 | 对齐变化 | 报「对齐变化」 | v09 |
| G09 | 行高变化（30 vs 默认） | 报「行高变化」 | v10 |
| G10 | 列宽变化（20 vs 默认） | 报「列宽变化」 | v11 |
| G11 | 合并单元格删除 | 报「合并删除」（A1 锚点位于合并区起点） | v12 |
| G12 | 公式文本变化（=1+1 vs =2+2） | 报「公式变化」 | v13 |
| G13 | 新增 sheet | 报 Sheet 级差异 | v14 |

## 2. 等价/豁免规则（2026-09-06 拍板已验证）
| # | 场景 | 判定 | 来源 |
|---|---|---|---|
| G20 | 浮点尾差（0.30000000000000004 vs 0.3） | 等价，不报（按显示精度 round 后比较） | B1/B4-2 |
| G21 | 公式大小写（=A1 vs =a1） | 等价，不报（normalize_formula 统一大写+去空白） | B1/B4-12 |
| G22 | 行高=默认 15（显式 15 vs 自动） | 豁免，不报 | B4-7 拍板 |
| G23 | 列宽=默认 8.43（显式 8.43 vs 自动） | 豁免，不报 | B4-8 拍板 |
| G24 | 字体别名组 | 等价，不报（内置组见下） | B4-4/B5-5 拍板 |
| G25 | 主题色（theme vs 等同 RGB） | 按显示色解析判定；theme=5(accent2)≠4472C4 为真差异 | B4-5/B5-1/B5-3 拍板 |
| G26 | 索引色等价（indexed=23 ↔ 808080 等） | 解析 RGB 后相等则等价 | B4-6/B5-4 |
| G27 | tint 舍入噪声 | HLS 回算 ±1 通道内视为等价（rgb_channel_close） | 引擎设计 |
| G28 | 调色板吸附 | 一侧 indexed 且 RGB 欧氏距离≤60 视为同色 | 引擎设计 |
| G29 | 数组/共享公式 | 按锚点公式文本比较；成员格缓存值差异忽略 | 引擎设计 |
| G30 | 富文本段落数不同 | 报「富文本变化」（内容相同的同段落不报） | B1-2 |
| G31 | 值检测关闭（value=False） | 普通值差不报；公式状态/文本差仍报（formula 开时） | B3-3/4 拍板 |
| G32 | 全检测关闭 | 全部不报（含数组公式分支） | B3-B |

## 3. 字体别名内置组（font_equiv）
微软雅黑 / Microsoft YaHei / Microsoft YaHei UI
宋体 / SimSun / NSimun（NSimSun）/ 新宋体
黑体 / SimHei
楷体 / KaiTi / 楷体_GB2312
仿宋 / FangSong / 仿宋_GB2312
等线 / DengXian / 等线 Light / DengXian Light

## 4. 规则判定（高级审核）
| # | 规则 | 判定 |
|---|---|---|
| G40 | range/offset/intersection 规则 | 数据源定位命中→逐检查项：expect=same 全部通过才豁免；expect=different 有差才豁免 |
| G41 | shift 规则 | 旧列+shift_offset=新列配对；行高/列宽 diff 只跑对应检查；标题行按行序配对；旧数据列外空列跳过 |
| G42 | 规则未命中的 diff | 不豁免（照实报） |
| G43 | 样式类 diff 无规则命中 | 用 COM 数据复核（com_style），显示一致则豁免（规则名=「样式显示一致」） |

## 5. 业务结论（用户拍板）
- Oven profile：整批 + 6 天 = 预期差异（不报异常）
- 模式 ID（P001...）由 ai_summary 动态生成，按数量降序

## 6. 观察中（待后续确认）
- COM 观察标签"程序:报差/无差"目前取设计值，需改为引擎实际结果（FEEDBACK 阶段修）
- 主题色 vs 等值 RGB 的 COM 色号差异（如 5066944 vs 12874308）：视觉同色但内部色号不同，
  当前按"解析后 RGB 相等"判定，COM 只作观察对照
